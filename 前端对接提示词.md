# 前端对接「管理员删除学生」功能

## 接口信息

| 项目 | 值 |
|---|---|
| 请求方式 | **POST** |
| 接口路径 | `/api/student/studentDelete` |
| 权限要求 | **ADMIN**（管理员角色） |
| Content-Type | `application/json` |

## 请求示例

```javascript
// 使用 axios 或 fetch 调用
const response = await axios.post('/api/student/studentDelete', {
  personId: 1  // 要删除的学生 personId
});
```

## 成功响应

```json
{
  "code": 0,
  "msg": "操作成功"
}
```

## 失败响应

```json
{
  "code": 1,
  "msg": "学生不存在"   // 或其他错误信息
}
```

## 前端调用代码示例（Vue + Element UI）

```vue
<template>
  <el-button type="danger" size="small" @click="handleDelete(row)">
    删除
  </el-button>
</template>

<script>
import axios from 'axios'

export default {
  methods: {
    async handleDelete(row) {
      // 二次确认弹窗
      await this.$confirm(`确定删除学生「${row.name}」吗？`, '提示', {
        confirmButtonText: '确定',
        cancelButtonText: '取消',
        type: 'warning'
      })

      try {
        const res = await axios.post('/api/student/studentDelete', {
          personId: row.personId
        })

        if (res.data.code === 0) {
          this.$message.success('删除成功')
          // 刷新列表
          this.getList()
        } else {
          this.$message.error(res.data.msg || '删除失败')
        }
      } catch (err) {
        this.$message.error('网络错误，请稍后重试')
      }
    }
  }
}
</script>
```

## 注意事项

1. **权限**：该接口需要 ADMIN 角色，前端需确保 token 中包含 ADMIN 权限
2. **级联删除**：后端会同时删除 student、user、person 三张表的数据，前端无需额外处理
3. **事务保护**：三表删除在同一个事务中，任一失败都会全部回滚，数据不会出现不一致
4. **删除顺序**：先删 student → 再删 user → 最后删 person